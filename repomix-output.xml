This file is a merged representation of the entire codebase, combined into a single document by Repomix.

<file_summary>
This section contains a summary of this file.

<purpose>
This file contains a packed representation of the entire repository's contents.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.
</purpose>

<file_format>
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
5. Multiple file entries, each consisting of:
  - File path as an attribute
  - Full contents of the file
</file_format>

<usage_guidelines>
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.
</usage_guidelines>

<notes>
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Files are sorted by Git change count (files with more changes are at the bottom)
</notes>

</file_summary>

<directory_structure>
pom.xml
src/main/java/br/edu/fema/controller/CursoController.java
src/main/java/br/edu/fema/controller/dto/CursoDto.java
src/main/java/br/edu/fema/controller/dto/UsuarioDto.java
src/main/java/br/edu/fema/controller/dto/UsuarioForm.java
src/main/java/br/edu/fema/controller/UsuariosController.java
src/main/java/br/edu/fema/FemaApplication.java
src/main/java/br/edu/fema/model/Curso.java
src/main/java/br/edu/fema/model/Usuario.java
src/main/java/br/edu/fema/repository/CursoRepository.java
src/main/java/br/edu/fema/repository/UsuarioRepository.java
src/main/java/org/yourcompany/yourproject/ProjectName.java
src/main/resources/application.properties
src/main/resources/data.sql
</directory_structure>

<files>
This section contains the contents of the repository's files.

<file path="pom.xml">
<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>3.1.5</version>
		<relativePath/>
	</parent>
	<groupId>br.edu.fema</groupId>
	<artifactId>forum</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name>fema-forum</name>
	<properties>
		<java.version>17</java.version>
	</properties>
	<dependencies>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-jpa</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-web</artifactId>
		</dependency>
		<dependency>
			<groupId>com.h2database</groupId>
			<artifactId>h2</artifactId>
			<scope>runtime</scope>
		</dependency>
	</dependencies>
	<build>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
			</plugin>
		</plugins>
	</build>
</project>
</file>

<file path="src/main/java/br/edu/fema/controller/CursoController.java">
package br.edu.fema.controller;

import java.util.List;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RequestMethod;
import org.springframework.web.bind.annotation.RestController;

import br.edu.fema.controller.dto.CursoDto;
import br.edu.fema.model.Curso;
import br.edu.fema.repository.CursoRepository;

@RestController
public class CursoController {

    @Autowired
    private CursoRepository cursoRepository;

    @RequestMapping(value = "/cursos", method = RequestMethod.GET)
    public List<CursoDto> lista() {
        List<Curso> cursos = cursoRepository.findAll();
        return CursoDto.converter(cursos);
    }

    @RequestMapping(value = "/cursos", method = RequestMethod.POST)
    public void cadastrar(@RequestBody Curso curso) {
        // Define o valor fixo no atributo "nome" antes de salvar no banco de dados
        curso.setNome("Curso de spring do Yago Siqueira Nascimento");
        cursoRepository.save(curso);
    }
}
</file>

<file path="src/main/java/br/edu/fema/controller/dto/CursoDto.java">
package br.edu.fema.controller.dto;

import java.util.List;
import java.util.stream.Collectors;

import br.edu.fema.model.Curso;

public class CursoDto {
    private Long id;
    private String nome;

    public CursoDto(Curso curso) {
        this.id = curso.getId();
        this.nome = curso.getNome();
    }

    public Long getId() { return id; }
    public String getNome() { return nome; }

    public static List<CursoDto> converter(List<Curso> cursos) {
        return cursos.stream().map(CursoDto::new).collect(Collectors.toList());
    }
}
</file>

<file path="src/main/java/br/edu/fema/controller/dto/UsuarioForm.java">
package br.edu.fema.controller.form;

import br.edu.fema.model.Usuario;

public class UsuarioForm {
    private String nome;
    private String email;
    private String senha;

    public String getNome() { return nome; }
    public String getEmail() { return email; }
    public String getSenha() { return senha; }

    // Isso transforma a ficha de cadastro em um Usuário de verdade
    public Usuario converter() {
        return new Usuario(nome, email, senha);
    }
}
</file>

<file path="src/main/java/br/edu/fema/FemaApplication.java">
package br.edu.fema;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class FemaApplication {

	public static void main(String[] args) {
		SpringApplication.run(FemaApplication.class, args);
	}
}
</file>

<file path="src/main/java/br/edu/fema/model/Curso.java">
package br.edu.fema.model;

import jakarta.persistence.Entity;
import jakarta.persistence.GeneratedValue;
import jakarta.persistence.GenerationType;
import jakarta.persistence.Id;

@Entity
public class Curso {
    @Id @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;
    private String nome;

    // Getters e Setters
    public Long getId() { return id; }
    public void setId(Long id) { this.id = id; }
    
    public String getNome() { return nome; }
    public void setNome(String nome) { this.nome = nome; }
}
</file>

<file path="src/main/java/br/edu/fema/repository/CursoRepository.java">
package br.edu.fema.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import br.edu.fema.model.Curso;

public interface CursoRepository extends JpaRepository<Curso, Long> {
}
</file>

<file path="src/main/java/org/yourcompany/yourproject/ProjectName.java">
/*
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 */

package org.yourcompany.yourproject;

/**
 *
 * @author yagos
 */
public class ProjectName {

    public static void main(String[] args) {
        System.out.println("Hello World!");
    }
}
</file>

<file path="src/main/resources/application.properties">
spring.datasource.url=jdbc:h2:mem:fema-forum
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.h2.console.enabled=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.defer-datasource-initialization=true
</file>

<file path="src/main/java/br/edu/fema/model/Usuario.java">
package br.edu.fema.model;

import jakarta.persistence.Entity;
import jakarta.persistence.GeneratedValue;
import jakarta.persistence.GenerationType;
import jakarta.persistence.Id;

@Entity
public class Usuario {
    @Id @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;
    private String nome;
    private String email;
    private String senha;

    // Construtor vazio (o Java obriga a ter)
    public Usuario() {}

    // Construtor que ensina como criar o usuário
    public Usuario(String nome, String email, String senha) {
        this.nome = nome;
        this.email = email;
        this.senha = senha;
    }

    // Getters (para o Java conseguir ler essas informações depois)
    public Long getId() { return id; }
    public String getNome() { return nome; }
    public String getEmail() { return email; }
    public String getSenha() { return senha; }
}
</file>

<file path="src/main/java/br/edu/fema/repository/UsuarioRepository.java">
package br.edu.fema.repository;

import java.util.List;

import org.springframework.data.jpa.repository.JpaRepository;

import br.edu.fema.model.Usuario; // Não te esqueças de importar a List

public interface UsuarioRepository extends JpaRepository<Usuario, Long> {
    
    // O Spring Boot vai traduzir isto para: SELECT * FROM usuario WHERE nome = ?
    List<Usuario> findByNome(String nome);
    
}
</file>

<file path="src/main/resources/data.sql">
INSERT INTO USUARIO(nome, email, senha) VALUES('Aluno', 'Aluno@email.com', '123456');
INSERT INTO USUARIO(nome, email, senha) VALUES('Yago Nascimento', 'yagosiqueira450@gmail.com', '123456');
</file>

<file path="src/main/java/br/edu/fema/controller/dto/UsuarioDto.java">
package br.edu.fema.controller.dto;

import java.util.List;
import java.util.stream.Collectors;

import br.edu.fema.model.Usuario;

public class UsuarioDto {
    private Long id;
    private String nome;
    private String email;

    // Aqui ele pega o usuário que veio da despensa e coloca na "embalagem"
    public UsuarioDto(Usuario usuario) {
        this.id = usuario.getId();
        this.nome = usuario.getNome();
        this.email = usuario.getEmail();
    }

    public Long getId() { return id; }
    public String getNome() { return nome; }
    public String getEmail() { return email; }

    public static List<UsuarioDto> converter(List<Usuario> usuarios) {
        return usuarios.stream().map(UsuarioDto::new).collect(Collectors.toList());
    }
}
</file>

<file path="src/main/java/br/edu/fema/controller/UsuariosController.java">
package br.edu.fema.controller;

import java.net.URI;
import java.util.List;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;
import org.springframework.web.util.UriComponentsBuilder;

import br.edu.fema.controller.dto.UsuarioDto;
import br.edu.fema.controller.form.UsuarioForm;
import br.edu.fema.model.Usuario;
import br.edu.fema.repository.UsuarioRepository;

@RestController
@RequestMapping("/usuarios")
public class UsuariosController {

    @Autowired
    private UsuarioRepository usuarioRepository; // A ligação com a despensa

    // O GET que você já tinha
    @GetMapping
    public List<UsuarioDto> lista(String nome) {
        if (nome == null) {
            List<Usuario> usuarios = usuarioRepository.findAll();
            return UsuarioDto.converter(usuarios);
        } else {
            List<Usuario> usuarios = usuarioRepository.findByNome(nome);
            return UsuarioDto.converter(usuarios);
        }
    }

    // A NOVIDADE: O POST para cadastrar
    @PostMapping
    public ResponseEntity<UsuarioDto> cadastrar(@RequestBody UsuarioForm form, UriComponentsBuilder uriBuilder) {
        // 1. Pega a ficha que o cliente mandou e vira um Usuário
        Usuario usuario = form.converter(); 
        
        // 2. Manda a despensa salvar no banco de dados
        usuarioRepository.save(usuario); 
        
        // 3. Devolve a resposta com "Status 201 Created" dizendo que deu certo
        URI uri = uriBuilder.path("/usuarios/{id}").buildAndExpand(usuario.getId()).toUri();
        return ResponseEntity.created(uri).body(new UsuarioDto(usuario));
    }
}
</file>

</files>
